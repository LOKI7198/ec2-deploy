name: "deploy-to-ec2"
description: "Index deployment with rsync/ssh"
author: "LOKI7198/deploy-to-ec2"
inputs:
  SSH_PRIVATE_KEY: # Private Key
    description: "Private Key"
    required: true
  REMOTE_HOST:
    description: "Remote host"
    required: true
  TARGET:
    description: "Target directory"
    required: false
    default: "/var/www/html"
